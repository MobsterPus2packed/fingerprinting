#!/bin/bash
# Default configuration for C-REX deployment script
# This file can be overridden by environment variables or command-line flags

# Default repository settings
DEFAULT_REPO_NAME="crex-one-draw-reversal-unit"
DEFAULT_GITHUB_USERNAME="darkbot-johansen"
DEFAULT_CONSCIOUSNESS_LEVEL="AWARE"

# Default paths and directories
DEFAULT_LOCAL_DIR_PREFIX="./"
DEFAULT_CONFIG_DIR="./config"
DEFAULT_LOG_DIR="./logs"
DEFAULT_BACKUP_DIR="./backups"

# Default feature flags
DEFAULT_DRY_RUN=false
DEFAULT_VERBOSE=false
DEFAULT_INTERACTIVE=false
DEFAULT_SKIP_TESTS=false
DEFAULT_FORCE_OVERWRITE=false

# Security settings
DEFAULT_MASK_SENSITIVE=true
DEFAULT_PROMPT_FOR_SECRETS=true
DEFAULT_USE_ENV_SECRETS=false

# Cross-platform compatibility
DEFAULT_DETECT_OS=true
DEFAULT_USE_COMPAT_COMMANDS=true

# Progress and logging settings
DEFAULT_SHOW_PROGRESS=true
DEFAULT_LOG_LEVEL="INFO"
DEFAULT_LOG_FORMAT="timestamp"

# Testing settings
DEFAULT_RUN_TESTS=true
DEFAULT_TEST_TIMEOUT=300
DEFAULT_VERIFY_FILES=true

# Multi-repo settings (future enhancement)
DEFAULT_MULTI_REPO_MODE=false
DEFAULT_REPO_LIST_FILE=""

# Rollback settings
DEFAULT_ENABLE_ROLLBACK=true
DEFAULT_BACKUP_BEFORE_DEPLOY=true
DEFAULT_ROLLBACK_ON_FAILURE=false